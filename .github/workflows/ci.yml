name: CI

on:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Clean, test, build, and verify plugin
        run: bash ./gradlew clean test buildPlugin verifyPluginProjectConfiguration verifyPlugin --no-daemon --no-build-cache

      - name: Upload plugin artifact
        uses: actions/upload-artifact@v4
        with:
          name: ti84-evo-plugin
          path: build/distributions/ti84-evo-for-pycharm-*.zip
          if-no-files-found: warn
